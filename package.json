{
  "name": "taskflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --pretty",
    "clean": "tsc -b --clean"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "^7.1.4"
  }
}
